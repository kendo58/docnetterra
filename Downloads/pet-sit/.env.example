# Copy to `.env.local` for local development.

# ------------------------------
# App
# ------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Optional but recommended for production split-deploy:
# Dedicated admin origin (e.g. https://admin.example.com). When set, `/admin/*` on the main app redirects there.
NEXT_PUBLIC_ADMIN_APP_URL=

# ------------------------------
# Supabase (required)
# ------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional server-side aliases (some tooling expects these)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Storage (Supabase Storage)
# Defaults to `uploads` if unset.
SUPABASE_STORAGE_BUCKET=uploads

# ------------------------------
# Stripe (required for payments)
# ------------------------------
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# Keep false in production unless you intentionally allow manual/test booking payments.
ALLOW_MANUAL_BOOKING_PAYMENTS=false

# ------------------------------
# Admin
# ------------------------------
# Admin authentication bypass is disabled in all environments.
# Use `npm run admin:create` (or `/admin/setup` in local development) to bootstrap access.

# ------------------------------
# Email (optional)
# ------------------------------
RESEND_API_KEY=
# SMTP (re-use Supabase Auth SMTP settings for app emails)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_SECURE=

# ------------------------------
# Analytics (optional - PostHog)
# ------------------------------
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ------------------------------
# Observability (optional - Sentry)
# ------------------------------
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
SENTRY_ENVIRONMENT=
SENTRY_RELEASE=

# ------------------------------
# Dev helpers (optional)
# ------------------------------
NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=http://localhost:3000/dashboard

# ------------------------------
# Testing (optional)
# ------------------------------
# For script-based database integration tests (runs against a real Supabase project).
SUPABASE_TEST_URL=
SUPABASE_TEST_SERVICE_ROLE_KEY=

# ------------------------------
# Background jobs (optional)
# ------------------------------
# Run the worker with: `npm run worker`
# Set to `true` in production once worker deployment is active.
SITSWAP_WORKER_ENABLED=false
# Safety switch. Keep false in production to avoid logging email payloads if SMTP is misconfigured.
ALLOW_EMAIL_LOG_FALLBACK=true
JOBS_POLL_INTERVAL_MS=1000
JOBS_BATCH_SIZE=10
JOBS_LOCK_TIMEOUT_SECONDS=300
JOBS_HOUSEKEEPING_INTERVAL_MS=300000
JOBS_WORKER_ID=
